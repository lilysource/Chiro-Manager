name: Build Windows application

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
      - name: Show .NET SDK
        run: dotnet --info
      - name: Clean
        run: dotnet clean RobloxAccountManager.sln --configuration Release -p:Platform=x64
      - name: Restore
        run: dotnet restore RobloxAccountManager.sln -p:Platform=x64
      - name: Build
        run: dotnet build RobloxAccountManager.sln --configuration Release --no-restore -p:Platform=x64
      - name: Test
        run: dotnet test tests/RobloxAccountManager.Tests/RobloxAccountManager.Tests.csproj --configuration Release --no-build -p:Platform=x64
      - name: Publish
        run: dotnet publish src/RobloxAccountManager/RobloxAccountManager.csproj --configuration Release --runtime win-x64 --self-contained false --no-restore --output artifacts/publish -p:Platform=x64
      - name: Package
        shell: pwsh
        run: Compress-Archive -Path artifacts/publish/* -DestinationPath RobloxAccountManager-Windows-x64.zip
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: RobloxAccountManager-Windows-x64
          path: RobloxAccountManager-Windows-x64.zip
